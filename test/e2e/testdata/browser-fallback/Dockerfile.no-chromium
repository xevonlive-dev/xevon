# Test image WITHOUT chromium installed.
# Verifies that xevon gives a clear error message when no browser is available.

FROM golang:1.26-alpine AS builder

RUN apk add --no-cache git make

WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .

RUN CGO_ENABLED=0 GOOS=linux go build \
    -ldflags "-s -w" \
    -o /out/xevon ./cmd/xevon

# Runtime: Debian WITHOUT chromium
FROM debian:bookworm-slim

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && \
    apt-get install -y --no-install-recommends \
        ca-certificates \
        dumb-init \
    && rm -rf /var/lib/apt/lists/*

COPY --from=builder /out/xevon /usr/local/bin/xevon

WORKDIR /root
ENTRYPOINT ["dumb-init", "--"]
