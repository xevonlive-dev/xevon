id: flask-method-decorator
language: python
severity: info
message: "Route: $METHOD $PATH"
rule:
  pattern: |
    @$APP.$METHOD($PATH)
    def $FUNC($$$PARAMS):
      $$$BODY
constraints:
  METHOD:
    regex: "^(get|post|put|delete|patch)$"
