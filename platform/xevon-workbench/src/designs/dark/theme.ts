/**
 * Dark theme color exports — re-exported from the shared domain palette.
 * Domain hues (severity, method, status, confidence) are fixed across themes
 * by design and live in `src/lib/domainPalette.ts` as CSS-var references.
 */
export {
  SEVERITY_COLORS,
  METHOD_COLORS,
  STATUS_COLORS,
  CONFIDENCE_COLORS,
  PROTOCOL_COLORS,
  MODULE_TYPE_COLORS,
  CHART_COLORS,
  AG_GRID_THEME,
} from '@/lib/domainPalette';
