interface Props {
  xevonVersion?: string;
  generatedAt?: string;
}

export default function Footer({ xevonVersion, generatedAt }: Props) {
  const version = xevonVersion ?? "v0.1.0-alpha";
  const stamp = generatedAt ?? "2026-04-18T18:33:55.082Z";

  return (
    <footer
      className="footer no-print wrap"
      style={{
        paddingTop: 0,
        marginTop: 0,
        borderTop: "none",
        flexDirection: "column",
        alignItems: "stretch",
      }}
    >
      <span
        style={{
          textAlign: "center",
          fontStyle: "italic",
          marginBottom: 4,
        }}
      >
        All findings are preliminary and have not yet gone through the confirmation stage or human
        review. They may include intended functionality or false positives.
      </span>
      <div
        style={{
          borderTop: "1px solid var(--v-border)",
          paddingTop: 14,
          display: "flex",
          justifyContent: "space-between",
          flexWrap: "wrap",
          gap: 10,
        }}
      >
        <span>
          Generated by xevon {version}
          {" · "}
          <span style={{ color: "var(--v-error)" }}>{stamp}</span>
        </span>
        <span>
          Develop by @codiologies
        </span>
      </div>
    </footer>
  );
}
